# TimesFM Ultra-Lightweight pour Railway/Render
# CORRECTED: Séparer PyTorch CPU des autres packages

# Dependencies standard depuis PyPI
timesfm>=1.0.0
numpy>=1.21.0,<2.0.0
psutil>=5.8.0

# PyTorch CPU-only sera installé séparément dans le Dockerfile
# torch est exclu d'ici pour éviter les conflits d'index

# SUPPRIMÉ pour économiser l'espace :
# - pandas (lourd, optionnel pour le serveur)
# - scipy (inclus avec timesfm)
# - scikit-learn (inclus avec timesfm) 
# - matplotlib (pas nécessaire pour serveur)
# - jupyter, notebook, etc.
