# TimesFM 1.0 - Memory Optimized for Render (512MB)
timesfm>=1.0.0
torch>=2.0.0
numpy>=1.21.0
psutil>=5.8.0

# Minimal dependencies only - removed heavy packages
# pandas removed (not essential for basic forecasting)
# scipy removed (timesfm handles math)
# matplotlib removed (no visualization in server)
