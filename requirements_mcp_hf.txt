# TimesFM 2.5 MCP Server via HuggingFace API
# Ultra-lightweight - zero model loading!

mcp>=1.0.0
httpx>=0.24.0
asyncio-mqtt>=0.13.0

# Optional: for enhanced logging
psutil>=5.8.0

# No torch, no timesfm, no numpy!
# Everything handled by HuggingFace API
